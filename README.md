# EF-Informatik

```py
print('Hello World')
```
--> Hello World


## Clone Git Repo

1. Open Terminal at required location in dir.
2. `git clone https://github.com/GraficPixelTDSM/EF-Informatik.git`
3. open vscode terminal
4. enter `git config --global user.name "USERNAME"` and `git config --global user.email "USEREMAIL"`